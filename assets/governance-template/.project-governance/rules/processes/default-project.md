# Default Project Process

本流程适用于非 UI 项目。若项目包含用户界面，优先使用 `ui-project.md`。

## 阶段

1. PRD 与架构确认。
2. 契约确认：API、模块接口、输入输出、副作用、数据模型或兼容性。
3. 核心实现。
4. 测试验证。
5. 当前版本全量验收。

## 强制规则

- 有歧义必须执行追问协议。
- 需求变化必须更新 PRD。
- 架构、契约、数据模型变化必须更新对应 SSOT。
- 当前版本完成前必须获得用户明确验收。
- 跳过阶段必须由用户确认。
